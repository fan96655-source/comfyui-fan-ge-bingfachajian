# 安全要求

1. 完整 API Key 只存在后端。
2. 前端只能看到供应商、标签和非秘密标识。
3. 不把 Admin Key 写进工作流。
4. 不打印 Authorization Header。
5. 不直接把裸 ComfyUI 暴露到公网。
6. 局域网访问也应增加反向代理、身份验证和防火墙。
7. `/view` 是本地文件链接，不是永久分享链接。
8. 重跑历史任务时，不恢复已被 Core 清理或脱敏的凭据。
9. 供应商查询失败只显示警告，不中断本地工作流。
